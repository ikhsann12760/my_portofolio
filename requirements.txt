Flask==2.3.0
Flask_Mail==0.9.1
gunicorn==20.1.0

# Notes:
# - `Flask` and `Flask-Mail` are required by `app.py` and Netlify function.
# - `Pillow` is required by `create_favicon.py` (uses `PIL`).
# - `gunicorn` is commonly used for production deployment (optional on Windows).
